# Core ML and data stack for the MoRE project
# For CUDA training, install torch from the PyTorch CUDA index instead of the default CPU wheel:
#   pip install --index-url https://download.pytorch.org/whl/cu126 torch==2.6.0

torch==2.6.0
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
wandb>=0.17
datasets>=2.18
transformers>=4.40
tokenizers>=0.19
nltk>=3.9
scikit-learn>=1.5

# Optional but commonly used in data/analysis scripts
# tqdm
